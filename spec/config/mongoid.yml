development:
  clients:
    default:
      database: resource_registry_development
      hosts:
        - localhost:27017
test:
  clients:
    default:
      database: resource_registry_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1